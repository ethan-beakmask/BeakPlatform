# Changelog 2024-12-20 14:15

## 變更摘要
安全性修正完成，所有 secure_code 現使用 `token_urlsafe(16)` 生成隨機 token。

## 完成項目

### 1. 安全性修正 - secure_code 格式
- 移除所有 migration SQL 中的硬編碼 secure_code
- 替換可猜測前綴 (`org_default_*`, `ADMIN_*`, `SYSTEM_ORG_*`)
- 建立 Python seed 腳本 (`scripts/seed_data.py`)

### 2. SQL 檔案清理
| 檔案 | 狀態 |
|------|------|
| `init_db.sql` | ✓ 移除 seed data |
| `001_menu_system.sql` | ✓ 移除 INSERT |
| `002_organization_contracts.sql` | ✓ 移除 INSERT |
| `003_menu_permissions.sql` | ✓ 移除 INSERT |
| `004_job_structure.sql` | ✓ 安全 (僅 DDL) |
| `005_seed_job_structure.sql` | ✓ 已廢棄 (.deprecated) |

### 3. CSRF Token 修正
- 28 個模板新增 CSRF token

### 4. Git Commit
```
80ff85d security: Replace guessable secure_code with token_urlsafe(16)
```
- 141 檔案變更
- +17,328 行 / -1,776 行

## Spec Check 結果
```
✅ Passed: 53 | Failed: 0 | Warnings: 39
```

警告類型：
- TENANT-01 (2): Organization/MenuPermission 不需 org_secure_code
- TENANT-02 (37): API 層直接使用 Model.query，待重構

## 下次作業項目

### 優先項目
1. **TENANT-02 重構** - 將 37 個 API 改用 ResourceGateway
2. **動態 Menu 前端渲染** - 整合 menu_service 到 base.html
3. **spec_check.py 改進** - 自動偵測專案根目錄

### 待辦項目
- 資料庫結構比對工具
- API 管理工具
- 完整 RBAC 權限系統

## 注意事項

### seed_data.py 使用方式
```bash
cd /opt/BeakMask
source venv/bin/activate
set -a && source .env && set +a
python scripts/seed_data.py
```

### spec_check.py 使用方式
```bash
cd /opt/BeakMask
source venv/bin/activate
python scripts/spec_check.py .
```

**注意**: 必須從專案根目錄執行，否則會找不到結構
