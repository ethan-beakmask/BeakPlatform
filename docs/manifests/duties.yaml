# 職務管理
# 職務類別、職務項目、依部門查詢
page: /api/duties/
description: 職務管理（類別 CRUD、職務 CRUD、依部門查詢）

files:
  api:      backend/app/api/duties.py
  model:    backend/app/models/duty.py           # DutyCategory + Duty
  template: null                                  # 純 API，無獨立頁面

tables:
  primary:
    - duty_categories
    - duties
  related:
    - organizational_units     # duty.unit_secure_code

security:
  decorator: "@admin_required"
  rls: true
  note: 純 API 端點，前端由其他頁面呼叫
